{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:typegen": "kysely-codegen --include-pattern='public.*' --camel-case --out-file src/database/types.d.ts"
  },
  "dependencies": {
    "hono": "^4.7.4",
    "kysely": "^0.27.6",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.11",
    "kysely-codegen": "^0.18.3"
  }
}
